Project TODOS:

* hello.service 
   x bundle with GreetingService and simple implementation (using a map)
   x exports GreetingService using DS
   x suitable for demoing start/stop (with SYSOUT in component)
   
* hello.jaxrs
   x bundle with sample JAX-RS resource
   x uses GreetingService (via injection)
   x test behavior when hello.service is started/stopped
   x should respond with 503 if service is not available
   x provides application using "JaxRSApplicationProviderComponent" from gyrex.http.jersey bundle
   - Optional: JSON view EclipseLink?
   
* gyrex.http.jersey
   x provides JAX-RS integration; will be part of Gyrex 1.1
   - need to provide ExceptionMapper for IllegalStateException -> 503
   x need to integrate Jersey Injection with Context + OSGi services
   - Optional: should integrate with EclipseLink for JAXB/JSON support (if dependency is available?)
   
* hello.service.persistence
   x implementation of GreetingService which persists to MongoDB
   
* hello.job
   - Bundle mit Job für verteiles Processing
   
* hello.feature
   - grouping unit of deployment, i.e. groups all bundles together
   - can be exported with PDE
   
DEMO / Ad-Hoc Cloud
-------------------

* kurze pause vorher
* Windows als Server oder besser Linux?
* Wichtig: p2 Installation funktioniert nicht aus der IDE heraus, als Standalone
* Client to demo "how-to-connect"
* Demo console administration using SSH connect
* FOLIE: Cloud-Connet Prozess (Tagging?)
* FOLIE: Workspace Location erklären und grobe Struktur
* export
* unterschied lokal (in lokale DB/ZK und dann gegen server)

